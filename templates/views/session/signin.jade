extends ../../layouts/default

block flash-messages
	//- intentionally blank
block content
	.container-xs.content-section.row
		.page-intro.col-md-6.col-md-offset-3
			h1(style='text-align: center') Sign into Upscale
			+flash-messages(messages)
		.page-content.col-md-6.col-md-offset-3
			+auth-forms({ modal: false, initial: 'signin' })

block js
	script.
		jQuery(function($) {
			$('.auth-form').find('.form-control').eq(0).focus().select();
		});
